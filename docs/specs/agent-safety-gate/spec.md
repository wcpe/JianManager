# 功能规格：Agent 入口可证安全闸

> 状态：草拟（并行前置）　·　关联 PRD：FR-388　·　依赖：FR-384/385/386

## 1. 背景与目标

把「默认只读 + 写白名单 + scope + 硬拒绝」做成**可证**：三入口同策略、CI 闸、真机矩阵。

## 2. 需求（要什么）

### 范围内
- 契约表：每个 action → 默认策略 / 写白名单归属 / 硬拒绝 / scope 字段 / 错误码 / 审计字段
- 三入口同策略矩阵：CLI ↔ MCP ↔ curl
- 真机复验矩阵 ≥30 断言（含：scope 外、吊销、过期、写白名单外、硬拒绝面、并发写、速率限制）
- CI：`release.yml` 或等价增加 agent-gate 测试闸
- 契约入 `docs/API.md`

### 不做
- 不替代 FR-384 实现策略；本 FR 验证与门禁

## 3. 设计（怎么做）

- 优先自动化测试枚举策略表 vs 路由注册
- 真机：docker-compose CP+Worker+实例；证据 `.tmp/acceptance-agent-gate-<date>.md`（不入库）

## 4. 任务拆分

- [ ] 契约表文档 + 代码枚举脚本
- [ ] 集成/API 测试 ≥30 条
- [ ] CI 闸
- [ ] 真机复验 runbook + 证据
- [ ] API.md 同步

## 5. 验收标准

1. 契约与代码一致（脚本 diff 空）
2. 三入口同 Token 同输入行为一致
3. 真机矩阵全过
4. CI 失败可拦截回归

## 6. 风险 / 待定

- 须在 FR-384/385/386 落地后执行；可先写契约与 mock 测
