# 前端构建产物（含 index.html 与 assets/*）由 `make embed-web` / vite build 注入，不入库。
# 本 .gitignore 占位保证目录存在，使 `//go:embed all:dist` 在清洁克隆下仍可编译；
# 未注入 index.html 时 RegisterStaticRoutes 使用 fallbackIndexHTML，router 测试不受影响。
# 正式发布/部署必须先执行 make build / make dist（内含 embed-web）。
*
!.gitignore
