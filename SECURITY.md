# 安全说明：JianManager

> 信任模型、敏感数据处理、外部输入校验与漏洞报告渠道。安全模型变化时更新。

## 1. 信任模型

- **浏览器与公网客户端不可信**：所有管理 API 经 Control Plane 鉴权（JWT 用户会话）；不得假设来自局域网的请求天然可信。
- **Worker 半可信**：节点持有 enrollment 一次性 token 完成注册后，使用节点身份（UUID + secret）与 CP 通信；指令面走 gRPC（含反向隧道）。恶意 Worker 可危害其上游戏服与本机文件，但不应仅凭节点凭据获得平台管理员权限。
- **玩家客户端 / OTA 拉取端不可信**：客户端分发依赖 HTTPS 传输、拉取密钥鉴权与制品 sha256；限流与封禁见客户端分发安全相关 FR。
- **攻击面概览**：CP HTTP/API/WS、CP gRPC（Worker 接入）、Worker 本机 WS/gRPC（探针与直拨回退）、安装脚本下载通道、发布产物供应链。

## 2. 密钥与敏感数据

| 材料 | 存放与轮换 | 注意 |
|---|---|---|
| `jwt.secret` / `JIANMANAGER_JWT_SECRET` | 配置或环境变量；**生产必改** | 只签用户会话；勿提交仓库；Docker 默认值仅开发 |
| WS 令牌密钥 `jwt.ws_secret` | 生产可自动生成并落数据根 `etc/ws-token-secret.key`，经注册/心跳下发 Worker | 轮换后旧探针桥 token 可能失效，需按 DEPLOY 排查 |
| enrollment token (`jmet_…`) | 面板签发、一次性限时；仅 CLI/env 传入 | **禁止**写入镜像层、git、长期配置文件 |
| 节点 `node_secret` / 身份文件 | Worker 数据根 `etc/node-identity.json` | 备份与权限按主机机密管理 |
| 客户端拉取密钥 / 可逆加密密钥 | CP 侧哈希鉴权 + 可选加密副本；见 DEPLOY | 日志禁止打印明文密钥 |
| 对象存储 / 代理凭据 | 配置或面板加密存储 | 勿写入 Dockerfile `ENV` 明文生产密钥 |

**严禁**：在代码、镜像默认环境、截图或 issue 中硬编码生产密钥、完整 token、私钥。

**日志**：错误信息可含请求 ID / 资源 ID；禁止输出密码、Authorization 头、enrollment 明文、拉取密钥全文。

## 3. 外部输入

- HTTP JSON / 查询参数：经路由与服务层校验（类型、长度、枚举、所有权）；文件路径禁止目录穿越出实例/授权根。
- gRPC：Worker 身份绑定后的操作仍做实例/节点归属校验。
- 安装脚本：仅下载预期资产名；过小文件视为失败；自定义 `--download-url` 时由运维保证来源可信。
- 上传与 OTA：完整性校验（sha256 等）与权限模型见制品库 / 客户端分发文档。

## 4. 漏洞报告

若发现安全问题，请**不要**在公开 Issue 中粘贴可利用细节或密钥。

- 优先通过 GitHub 仓库的 **Security Advisories / 私密漏洞报告**（若已启用），或联系仓库维护者（GitHub `@` 维护者 / 项目 README 所示渠道）。
- 请包含：影响版本、复现步骤、影响面（是否需已鉴权、是否 RCE/越权）、是否已公开。
- 维护者将确认收悉并评估修复与披露节奏；请预留合理修复窗口后再公开细节。
