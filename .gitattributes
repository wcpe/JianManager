# 仓库换行规范（v0.9.0 走查 #18）：所有文本以 LF 存储与检出，
# 杜绝 Windows CRLF ↔ LF 往返反复污染 diff（开发期满屏「LF will be replaced by CRLF」）。
* text=auto eol=lf

# protoc 生成物（#17）：标记为生成代码——PR diff 默认折叠、不计入语言统计；
# 多分支并行各自 regen 撞车时按约定从合并后的 .proto 用 protoc 重生成（非手改/非 union 拼接，
# 拼接会产生重复声明无法编译）。pb.go 仍入库以便无 protoc 环境构建。
proto/workerpb/*.pb.go linguist-generated=true

# 文本类显式 LF（生成器/脚本产物，避免被平台默认改写）。
*.go    text eol=lf
*.ts    text eol=lf
*.tsx   text eol=lf
*.js    text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
*.sh    text eol=lf
*.svg   text eol=lf
*.ps1   text eol=lf crlf=input

# 二进制：不做文本/换行转换。
*.jar   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
