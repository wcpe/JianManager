/**
 * 监控页占位桩（FR-169，由 W2 实现）。
 *
 * 本文件仅为 W1（外壳/双主题）提供编译期占位，使 `/monitor` 路由与构建通过；
 * 真实平台/节点/实例监控仪表骨架由 worktree W2 落地后整体替换本文件。
 * TODO(W2/FR-169): 替换为监控仪表骨架（6 指标 + brush + 进程 TOP10 + 实时）。
 */
export default function MonitoringPage() {
  return null
}
